import React from 'react';
import ReactDOM from 'react-dom/client';
import KeygenUI from './KeygenUI';
import '../renderer/styles/index.css';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <KeygenUI />
  </React.StrictMode>
);
